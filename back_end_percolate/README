Setup requirements:

python2.6
mock==1.0.1
simplejson
nose


To run:
	At root folder(./back_end_percolate):
	python rolodex/src/rolodex.py -i data.in -o data.out

To test:
	At root folder(./back_end_percolate):
	# One test case relies on hardcoded file path, running this elsewhere will get you a file not found error
	nosetests rolodex/tests/*.py

# There might be linting issues, guess that's not the key point here. :)
